# requirements_optimized.txt
# Minimal dependencies for real-time ClickHouse ingestion

# For connecting to ClickHouse database
clickhouse-driver

# For WebSocket client connections  
websockets

# For HTTP requests to ClickHouse
requests

# For environment variables (lightweight)
python-dotenv

# Removed heavy dependencies:
# - kafka-python (replaced with direct HTTP)
# - pandas (replaced with native clickhouse-driver for verification)  
# - pyarrow (only used for unused export functionality)